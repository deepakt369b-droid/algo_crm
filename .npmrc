# Allow install to proceed when package.json has changed but lockfile hasn't been updated yet.
# This is needed during CI/Cloudflare Pages builds until the lockfile is regenerated.
# Run `pnpm install` locally and commit the updated pnpm-lock.yaml for reproducibility.
frozen-lockfile=false
